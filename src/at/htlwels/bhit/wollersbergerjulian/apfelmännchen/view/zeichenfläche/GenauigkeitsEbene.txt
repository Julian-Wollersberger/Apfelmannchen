package at.htlwels.bhit.wollersbergerjulian.apfelmännchen.view.zeichenfläche

import javafx.scene.layout.Region

// Created by julian on 23.07.17.
/**
 * Es ist eine Region und kein Pane, damit andere Klassen
 * die Children nicht verändern dürfen.
 */
class GenauigkeitsEbene : Region() {

    //TODOO Override LayoutChildren
    /** Anforderungen:
     * Größe Ändern:
     * -Der Bereich, der aktuell sichbar ist, muss sichtbar bleiben.
     * -Was dieser Bereich ist, soll nach dem loslassen zurückgesetzt werden.
     * -(Beibehalten des Seitenverhältnisses)
     * -bzw. an den entstehenden leeren Flächen weiterzeichnen.

     * Zoomen:
     * -Der Punkt, wo die Maus darüber ist, soll sich nicht ändern.
     * -Exponentieller Zoomfaktor: z.B. 10 gescrollte Zeilen sollen Verdopplung sein.
     * -Schnapp-Punkt bei Original-Größe.

     * Für später:
     * -nicht mehr im Bild befindliche Teile unsichtbar machen / löschen
     */
    override fun layoutChildren() {
        super.layoutChildren()
    }
}
